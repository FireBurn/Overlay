--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -128,24 +128,9 @@
   endif()
 
   # Link against LLVM and CLANG libraries.
-  target_link_libraries(hipify-clang PRIVATE
-    clangASTMatchers
-    clangFrontend
-    clangTooling
-    clangParse
-    clangSerialization
-    clangSema
-    clangEdit
-    clangFormat
-    clangLex
-    clangAnalysis
-    clangDriver
-    clangAST
-    clangToolingCore
-    clangRewrite
-    clangBasic
-    ${HIPIFY_ADDL_LINK_LIBS}
-  )
+  # Gentoo only ships the monolithic clang-cpp library, not the
+  # individual clang component libraries.
+  target_link_libraries(hipify-clang PRIVATE clang-cpp)
 
   if(LLVM_PACKAGE_VERSION VERSION_EQUAL "5.0.0" OR LLVM_PACKAGE_VERSION VERSION_GREATER "5.0.0")
     if(MSVC)
